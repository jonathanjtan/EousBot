node_modules/
dist/
.env
.env.local
*.log

# Personal deployment context: host names, resource groups, costs. This repo is
# public, so machine-specific detail stays out of README.md and lives here.
CLAUDE.local.md

# Runtime state (pending redeploy announcements, in-flight build records)
state/

# Agent worktrees are created here and cleaned up after each build
.worktrees/

.DS_Store
